# Ingorokva Clinic Medical Hub

Source for the Ingorokva Clinic site.

## Run locally

Prerequisite: Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`
3. Build the GitHub Pages-ready site into the repository root:
   `npm run build`
